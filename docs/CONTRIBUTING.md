# 🤝 Руководство для контрибьюторов

Спасибо за интерес к проекту! Это руководство поможет вам внести вклад в развитие Исламского Калькулятора Наследства.

---

## Быстрый старт

```bash
# 1. Fork репозитория на GitHub

# 2. Клонирование
git clone https://github.com/YOUR_USERNAME/sahih-inherit.git
cd sahih-inherit

# 3. Установка зависимостей
npm install

# 4. Запуск тестов
npm test

# 5. Создание ветки
git checkout -b feature/my-feature

# 6. Внесение изменений...

# 7. Коммит
git commit -m "feat: добавлена новая функция"

# 8. Push
git push origin feature/my-feature

# 9. Создание Pull Request на GitHub
```

---

## Структура проекта

```
sahih-inherit/
├── index.html              # Основное приложение (single-page)
├── src/                    # Модули расчетного движка
│   ├── fraction.js         # Класс для работы с дробями
│   ├── shares.js           # Расчет фиксированных долей
│   ├── blocking.js         # Система блокировки
│   └── special-cases.js    # Особые случаи
├── tests/                  # Тесты
│   ├── unit/               # Unit-тесты
│   ├── property/           # Property-based тесты
│   ├── integration/        # Интеграционные тесты
│   └── comprehensive/      # Комплексные сценарии
├── docs/                   # Документация
└── .kiro/specs/            # Спецификации проекта
```

---

## Типы вкладов

### 🐛 Исправление багов

1. Создайте issue с описанием бага
2. Форкните репозиторий
3. Создайте ветку `fix/issue-number`
4. Исправьте баг
5. Добавьте тест, воспроизводящий баг
6. Создайте PR

### ✨ Новые функции

1. Обсудите функцию в issues
2. Дождитесь одобрения
3. Создайте ветку `feature/feature-name`
4. Реализуйте функцию
5. Добавьте тесты (unit + property)
6. Обновите документацию
7. Создайте PR

### 📚 Документация

1. Создайте ветку `docs/topic`
2. Внесите изменения
3. Создайте PR

### 🧪 Тесты

1. Создайте ветку `test/what-testing`
2. Добавьте тесты
3. Создайте PR

---

## Стандарты кода

### JavaScript

```javascript
// ✅ Хорошо
/**
 * Рассчитывает долю мужа
 * @param {object} heirs - состояние наследников
 * @returns {Fraction} - доля мужа
 */
export function calculateHusbandShare(heirs) {
  return hasChildren(heirs) ? new Fraction(1, 4) : new Fraction(1, 2);
}

// ❌ Плохо
function calc(h) {
  return h.sons > 0 ? new Fraction(1, 4) : new Fraction(1, 2);
}
```

### Правила

1. **Используйте JSDoc** для документирования функций
2. **Используйте понятные имена** переменных и функций
3. **Пишите комментарии** на русском языке
4. **Следуйте существующему стилю** кода
5. **Избегайте магических чисел** — используйте константы

### CSS

```css
/* ✅ Хорошо */
.results-table__header {
  background-color: var(--color-primary);
  padding: var(--spacing-md);
}

/* ❌ Плохо */
.header {
  background: #1a5f4a;
  padding: 16px;
}
```

### Правила

1. **Используйте CSS Variables** для цветов и отступов
2. **Используйте BEM** для именования классов
3. **Mobile-first** — начинайте с мобильных стилей

---

## Коммиты

### Формат сообщения

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

### Типы

| Тип        | Описание                           |
| ---------- | ---------------------------------- |
| `feat`     | Новая функция                      |
| `fix`      | Исправление бага                   |
| `docs`     | Документация                       |
| `style`    | Форматирование (не влияет на код)  |
| `refactor` | Рефакторинг                        |
| `test`     | Добавление тестов                  |
| `chore`    | Обслуживание (зависимости, конфиг) |

### Примеры

```bash
# Новая функция
git commit -m "feat(shares): добавлен расчет доли прадеда"

# Исправление бага
git commit -m "fix(blocking): исправлена блокировка единокровных сестер"

# Документация
git commit -m "docs: добавлено руководство по тестированию"

# Тесты
git commit -m "test(fraction): добавлены property-тесты для деления"
```

---

## Pull Request

### Чеклист

- [ ] Код соответствует стандартам проекта
- [ ] Все тесты проходят (`npm test`)
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация (если нужно)
- [ ] Коммиты следуют конвенции
- [ ] PR имеет понятное описание

### Шаблон описания PR

```markdown
## Описание

Краткое описание изменений.

## Тип изменения

- [ ] Исправление бага
- [ ] Новая функция
- [ ] Критическое изменение (breaking change)
- [ ] Документация

## Как тестировать

1. Шаг 1
2. Шаг 2
3. Ожидаемый результат

## Связанные issues

Closes #123
```

---

## Тестирование

### Требования

1. **Все новые функции** должны иметь тесты
2. **Property-тесты** для универсальных правил
3. **Unit-тесты** для граничных случаев
4. **Минимум 100 итераций** для property-тестов

### Запуск тестов

```bash
# Все тесты
npm test

# С покрытием
npm test -- --coverage

# Конкретный файл
npm test -- tests/property/shares.test.js
```

### Пример теста

```javascript
/**
 * Property N: [Название]
 * [Описание]
 * Validates: Requirements X.Y
 */
test("Property N: [Название]", () => {
  fc.assert(
    fc.property(arbitraryHeirsState, (heirs) => {
      const result = someFunction(heirs);
      return someProperty(result);
    }),
    { numRuns: 100 }
  );
});
```

---

## Добавление нового наследника

### 1. Обновите `shares.js`

```javascript
// Добавьте в HeirTypes
NEW_HEIR: {
  id: "newHeir",
  nameRu: "Новый наследник",
  nameAr: "...",
  category: "...",
  gender: "...",
  isAsaba: false,
  getShare: (heirs) => {
    // Логика расчета доли
  },
  dalil: "Источник"
}
```

### 2. Обновите `blocking.js` (если нужно)

```javascript
// Добавьте правило блокировки
newHeir: (heirs) => heirs.someBlocker === true,
```

### 3. Обновите `index.html`

```html
<!-- Добавьте UI-элемент -->
<div class="form-group">
  <label class="checkbox">
    <input type="checkbox" id="newHeir" />
    <span class="checkbox__mark"></span>
    <span class="checkbox__label">Новый наследник</span>
  </label>
</div>
```

### 4. Добавьте тесты

```javascript
// tests/property/shares.test.js
test("Property: Доля нового наследника корректна", () => {
  fc.assert(
    fc.property(arbitraryHeirsState, (heirs) => {
      // Тест
    }),
    { numRuns: 100 }
  );
});
```

### 5. Обновите документацию

- `docs/ISLAMIC_LAW_REFERENCE.md` — правила расчета
- `README.md` — если нужно

---

## Вопросы и помощь

### Где задать вопрос?

1. **GitHub Issues** — для багов и предложений
2. **GitHub Discussions** — для вопросов и обсуждений

### Полезные ресурсы

- [Спецификации проекта](.kiro/specs/islamic-inheritance-calculator/)
- [Архитектура](docs/ARCHITECTURE.md)
- [Справочник по исламскому праву](docs/ISLAMIC_LAW_REFERENCE.md)
- [Руководство по тестированию](docs/TESTING_GUIDE.md)

---

## Кодекс поведения

1. **Уважайте** других участников
2. **Будьте конструктивны** в критике
3. **Помогайте** новичкам
4. **Следуйте** стандартам проекта

---

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

---

<div align="center">

**Джазакум Аллаху хайран за ваш вклад! 🤲**

</div>
